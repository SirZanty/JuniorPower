services:
  app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - db-data:/app/data
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ConnectionStrings__Default=Data Source=/app/data/juniorpower.db

volumes:
  db-data:
